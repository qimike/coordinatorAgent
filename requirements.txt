# Multi-Agent Research Pipeline — dependencies
#
# The pipeline itself uses only the Python standard library so it can run
# in any environment without network access. pytest is used for the
# reliability test-suite in tests/.
pytest>=7.0
